/* Deployment config. Leave blank for fully-offline builds — analytics then
   stays in a local ring buffer and nothing leaves the device. */
export const CONFIG = {
  supabaseUrl: '',       // e.g. 'https://xyzcompany.supabase.co'
  supabaseAnonKey: '',
};
